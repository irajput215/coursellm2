"""Evaluation runners.

``run_retrieval_eval`` is the LLM-free half that CI measures for real;
``run_rag_eval`` adds generation and judging; ``check_regression`` compares a
report against the committed baseline.
"""
